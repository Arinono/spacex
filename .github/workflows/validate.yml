name: Validate microservice

on: [push]

jobs:
  
  validate:
  
    runs-on: ubuntu-latest
    
    strategy:
      matrix:
        node-version: [12.x]
    
    steps:
      - uses: arinono/validate@v3
